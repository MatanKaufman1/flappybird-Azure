terraform init t
terraform apply -target=module.keyvault

export AZURE_SUBSCRIPTION_ID="your-subscription-id"


az keyvault secret set --vault-name "hdo3keyvault13" --name "AZURE-SQL-USERNAME" --value "adminuser"
az keyvault secret set --vault-name "hdo3keyvault13" --name "AZURE-SQL-PASSWORD" --value "your-secure-password"

az keyvault secret set --vault-name "hdo3keyvault13" --name "AZURE-SQL-SERVER" --value "azmysql"
az keyvault secret set --vault-name "hdo3keyvault13" --name "AZURE-SQL-DATABASE" --value "azmydb"

terraform apply
